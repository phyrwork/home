{
  "config": {
    "step": {
      "user": {
        "title": "Energy Cost Forecast",
        "data": {
          "name": "Name",
          "import_rate_sensor": "Import rate entity",
          "export_rate_sensor": "Export rate entity (optional)",
          "export_power_sensor": "Export power entity (optional)",
          "power_profile": "Appliance power profile (JSON/YAML list)",
          "profile_file": "Profile file path (optional)",
          "power_profile_sensor": "Profile sensor entity (optional)",
          "target_percentile": "Target percentile (0-100)",
          "start_step_mode": "Start step mode",
          "start_step_minutes": "Start step minutes",
          "update_interval_minutes": "Update interval minutes (0 disables)"
        }
      }
    },
    "error": {}
  }
}
